# Prompt 16.3 — Add Video Link + Swap .mov for .mp4

> **Character.** Small wrap-up. Add a "Watch full video →" link beneath the architecture gif in README, pointing to the GitHub-hosted .mp4. Also replace the bulky .mov in the repo with the compact .mp4.
>
> **Expected duration:** 10-15 minutes.
>
> **Budget:** Zero LLM cost.

---

## Outcome (session 16.3 — 2026-04-24)

Commit: `c8df00d`

**Fix 1 — README update:**

Added `**Full-fidelity video:**` label and GitHub-hosted URL on its own line beneath the gif, before the caption. Caption updated to reference both artifacts. Section now reads: gif (loops automatically, instant motion) → video link (click to play, full resolution) → caption.

**Fix 2 — File swap:**

- `.mov` (25MB) removed via `git rm`
- `.mp4` (325KB) added from `~/Desktop/architecture-animation.mp4`
- Net saving: ~25MB (98% reduction)
- `.mp4` is the GitHub-upload-transcoded version, not a local ffmpeg transcode

**Fix 3 — .gitignore:**

No `.mov` or `.mp4` patterns existed — no changes needed.

**Video URL used:**
`https://github.com/user-attachments/assets/11269e7f-f441-4288-97f0-2048d5f0b5e7`

**README section after edit:**
```markdown
## How PulseCraft works

![PulseCraft Architecture — ...](design/demo/gif/architecture-animation.gif)

**Full-fidelity video:**

https://github.com/user-attachments/assets/11269e7f-f441-4288-97f0-2048d5f0b5e7

*Three LLM agents, four guardrail hooks, one deterministic orchestrator. The gif above loops the pipeline end-to-end; the video plays it at full resolution. Explore the interactive version with `pulsecraft demo serve` and click into the Architecture tab.*
```

**Test count:** 642 passed (unchanged).
