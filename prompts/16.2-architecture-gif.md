# Prompt 16.2 — Embed Architecture Animation in README

> **Character.** Convert the user's `.mov` recording of the Architecture tab animation into an optimized gif, save it to the repo, and embed it at the top of README.md. Everything done by Claude Code autonomously.
>
> **Expected duration:** 30-45 minutes.
>
> **Budget:** Zero LLM cost.
>
> **Prerequisite:** `ffmpeg` installed locally.

---

## Outcome (session 16.2 — 2026-04-24)

Commit: `703ef60`

**Source recording:** `/Users/sucandra_dasa/Desktop/PulseCraft-Architecture.mov`
- Duration: 56s (full session including tab navigation, replay click, node exploration)
- Resolution: 2756×1390 @ 35fps
- Size: 25MB

**Recording structure (from frame inspection):**
- t=0–1.1s: Completed diagram visible (pre-replay static state)
- t=~1.3s: User clicks Replay button
- t=1.3–11.2s: Full 9.9s entrance animation
- t=13s+: User clicks nodes, scrolls through detail panels

**Trim window used:** `ss=1.1 -t 11.5` — captures from blank canvas through animation completion plus brief settled hold.

**GIF parameters:**
- 18fps (source is 35fps; 18fps is smooth for UI animation, ~half the frames)
- 1100px wide (source is 2756px; ~40% of original)
- 192 palette colors
- Bayer dithering scale=4 (clean flat-color UI dithering)
- Two-pass palette generation
- Output: 553KB (well under 2MB target)

**Quality verified:** Frame inspection at t=4s (mid-animation, BUAtlas appearing) and t=10s (completed diagram) confirmed crisp text, readable node labels, correct colors, visible stacked-card fan-out effect on BUAtlas.

**README placement:** New `## How PulseCraft works` section inserted between `## Overview` and `## Demo results`. Alt text describes full pipeline for accessibility.

**Also fixed:** Test badge updated from 637 to 642.

**Raw recording preserved:** `design/demo/videos/architecture-animation.mov` (25MB, committed)

**Test count:** 642 passed (unchanged).
