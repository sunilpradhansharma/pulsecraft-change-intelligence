# Prompt 03.5 — Session Continuity Setup

> **Why this exists.** Claude Code sessions don't persist context between runs. If you close a session and open a new one tomorrow, that session starts blind — it doesn't remember what we've built, where we are, or what conventions apply. The fix is to make the repo *self-documenting* so any new session can orient itself from what's in the repo. This prompt establishes that infrastructure.
>
> **How to use this prompt.**
> 1. Copy everything below the `---` line into Claude Code, running inside the repo.
> 2. Claude Code creates `CLAUDE.md`, updates the planning index, updates the root README, and fixes the `types-PyYAML` TODO from prompt 03.
> 3. Commits once. Offers to archive itself into `prompts/` at the end.
>
> **Expected duration:** 30 minutes.
>
> **Prerequisite:** Prompts 00–03 completed. 84 schema/config/fixture tests passing. Two recent commits: `0d8a2fe` (config + fixtures) and `d633a93` (prompt archive).
>
> **Why before prompt 04:** prompt 04 (orchestrator spec) will *extend* `CLAUDE.md`, so `CLAUDE.md` needs to exist with the right structure first.

---

# Instructions for Claude Code

You are setting up **session continuity** for the PulseCraft repository. The goal: any new Claude Code session — whether it's you in an hour or a different session three months from now — should be able to open this repo, read `CLAUDE.md`, and understand exactly where the project is, what conventions apply, and how to behave.

This prompt is purely housekeeping. No code logic, no new functionality. Three artifacts to produce, one TODO to fix, one commit.

## Environment discipline

Use `.venv/bin/python`, `.venv/bin/pytest`, `.venv/bin/ruff`, `.venv/bin/mypy` explicitly. Never rely on `source .venv/bin/activate` persisting between tool calls. If `uv` is available, `uv run <cmd>` is acceptable.

## What "done" looks like

When you finish:

1. `CLAUDE.md` exists at the repo root with standing instructions for every future Claude Code session.
2. `design/planning/00-planning-index.md` is updated to reflect reality (prompts 00–03 done, 04 next).
3. Root `README.md` shows the correct current phase.
4. `pyproject.toml` has `types-PyYAML` declared as a dev dependency (resolves prompt 03 TODO).
5. One commit with a clear message, plus the optional prompt archive commit.

## Step-by-step work

### Step 1 — Context review

Read these files so you know what's true right now:

- `README.md` at repo root (current state)
- `design/README.md`
- `design/planning/00-planning-index.md`
- `design/planning/01-decision-criteria.md` (just skim — you don't need to memorize it, just know it exists)
- `prompts/` — scan filenames to see what prompts have been run (expect 00, 01, 02, 03)

### Step 2 — Author `CLAUDE.md` at the repo root

This file is read automatically by Claude Code at the start of every session. It's *standing orders* — not project documentation, not a README, not a design doc. It tells future sessions how to behave in *this specific repo*.

Create `CLAUDE.md` with exactly this structure and content. Fill in the placeholders with current reality.

````markdown
# CLAUDE.md — Standing Instructions for Claude Code Sessions

> This file is read automatically at the start of every Claude Code session in this repo.
> It is the single source of truth for how a session should behave.
> **If you are a Claude Code session reading this for the first time: read this file completely before taking any action.**

---

## About this project

**PulseCraft** is an internal AI service that turns marketplace product/feature changes into BU-ready, personalized notifications for BU leadership. It's implemented as a team of three specialist AI agents — **SignalScribe**, **BUAtlas**, **PushPilot** — each acting as decision-makers at six judgment gates, orchestrated by a deterministic Python service built on the Claude Agent SDK.

**Sponsor:** Head of AI.
**Status:** Planning complete; implementation in progress via prompt-driven development.

## Current phase

<!-- Every prompt that lands a commit updates this section. -->

**Phase:** P3 — Agent prompt authoring (in progress)

**Prompts completed:**
- ✅ 00 — Repo scaffold + Python project setup
- ✅ 01 — Commit planning documents (problem statement, ADRs, decision criteria, architecture)
- ✅ 02 — JSON schemas + Pydantic models for data contracts
- ✅ 03 — Config files (BU registry, profiles, policy, channel policy) + synthetic change fixtures
- ✅ 03.5 — Session continuity setup (this prompt)

**Prompts remaining:**
- ⏳ 04 — CLAUDE.md orchestrator spec (extends this file with orchestrator section)
- ⏳ 05 — Agent: SignalScribe (gates 1, 2, 3)
- ⏳ 06 — Agent: BUAtlas (gates 4, 5, parallel per-BU)
- ⏳ 07 — Agent: PushPilot (gate 6)
- ⏳ 08 — Skills: ingest adapters
- ⏳ 09 — Skills: registry, policy, audit
- ⏳ 10 — Skills: delivery rendering
- ⏳ 11 — Operator slash commands
- ⏳ 12 — Guardrail hooks
- ⏳ 13 — First end-to-end dryrun
- ⏳ 14 — Eval harness

## Where to find context

Before doing any non-trivial work, read these (in this order):

1. **`design/README.md`** — architecture overview, agent roles, key properties
2. **`design/planning/01-decision-criteria.md`** — the six-gate agent judgment spec. **This is the source of truth for every agent prompt.** If any prompt or piece of code disagrees with this document, this document wins.
3. **`design/planning/00-planning-index.md`** — current phase, open decisions, open questions, prompt-driven build status
4. **`design/00-problem-statement.md`** — scope, constraints, scale envelope, assumptions, risks
5. **`design/adr/ADR-001-workflow-with-subagents.md`** — the pattern decision (workflow + agentic subagents, not peer agents, not monolith)
6. **`design/adr/ADR-002-subagent-topology.md`** — fan-out strategy, component-to-primitive map, decision rubric

## Build process — prompt-driven development

All implementation happens via prompts in `prompts/`, run one at a time in Claude Code.

- Each prompt produces a specific set of files + a single commit.
- Prompts are numbered (`00-`, `01-`, ..., `14-`). Run in order unless told otherwise.
- Each prompt is **self-contained** — it includes its own pre-flight checks, step-by-step instructions, verification, commit message, and final report format.
- Prompts are archived in `prompts/` after they run, so the repo carries the full build trail.

**Do not invent steps that aren't in the prompt.** If a prompt doesn't specify something, ask the user rather than improvise.

## Environment rules

- **Python version:** 3.14 (may pin to 3.13 later if Claude Agent SDK compatibility surprises us)
- **Virtual environment:** `.venv/` at repo root, created via `uv venv`. No `pip` binary inside — use `uv pip install` or `.venv/bin/python -m pip` (after installing pip into the venv).
- **Always invoke tools via `.venv/bin/`** — `.venv/bin/python`, `.venv/bin/pytest`, `.venv/bin/ruff`, `.venv/bin/mypy`. Never rely on `source .venv/bin/activate` persisting between tool calls — shell state may reset.
- **`uv run <cmd>`** is an acceptable alternative; it handles activation.
- **Package manager:** `uv` preferred. Fall back to `.venv/bin/python -m pip` if `uv` is unavailable.

## Project conventions

- **No real enterprise data.** No real product names, no real BU names (we use `bu_alpha` through `bu_zeta`), no real people (placeholders like `<head-alpha>`), no real internal system names. Real data lands via Track A discovery, not via Claude Code.
- **Placeholder pattern for unknowns:** use `<descriptor>` in angle brackets (e.g., `<head-alpha>`, `<delegate-1>`). Never invent realistic-sounding fake names.
- **Decision criteria is the source of truth.** For any agent behavior question, the answer comes from `design/planning/01-decision-criteria.md`. Do not encode conflicting rules elsewhere.
- **Schemas are invariant across agent iterations.** Agent prompts may change; data contracts (defined in `schemas/` and `src/pulsecraft/schemas/`) stay stable. If a schema change is genuinely needed, pause and ask rather than break the contract.
- **Snake case everywhere:** BU IDs (`bu_alpha`), file names (`change_001_*.json`), Python identifiers.
- **ISO-8601 UTC timestamps** everywhere. Never epoch seconds. Never local time.
- **No PII, PHI, or secrets** in any committed file — ever. This includes fixtures, examples, docstrings, test data.

## Commit conventions

- **Conventional commit prefixes:** `feat:`, `fix:`, `chore:`, `docs:`, `test:`, `refactor:`.
- **Commit message body** explains what, why, and references which prompt produced it.
- **Co-author attribution** — if Claude Code made the commit, include a `Co-Authored-By: Claude ...` trailer.
- **One prompt = one feature commit + optionally one prompt-archive commit.** Don't batch unrelated work.
- **Never force-push. Never rebase shared branches. Never push to remote without the user asking.**

## Testing norms

- Every prompt that adds code adds tests.
- Tests use `.venv/bin/pytest` explicitly.
- Tests live under `tests/unit/` or `tests/integration/`.
- Fixtures live under `tests/fixtures/` (for unit test fixtures) or `fixtures/` (for shared domain fixtures like change artifacts).
- Don't silently weaken a test to make it pass. If a test is genuinely wrong, fix the test with a clear commit message. If the test is right and the code is wrong, fix the code.
- **Enum parity tests between JSON schema and Python `StrEnum` are deliberately strict.** If one fails, the schemas and Pydantic models have drifted — fix the drift, don't weaken the test.

## After every commit — MANDATORY updates

Every prompt that lands a commit must, as part of the same session:

1. Update the **"Current phase"** section of this `CLAUDE.md` — add the completed prompt to the ✅ list, update "in progress" marker.
2. Update **`design/planning/00-planning-index.md`** — mark the prompt as done in the phase table and the prompt-workflow table; add to the "Completed artifacts" section.
3. Update the root **`README.md`** — current phase indicator stays in sync.

If a prompt authors **skills**, list them in the Skills section of this file.
If a prompt authors **commands**, list them in the Commands section of this file.

## Skills authored so far

<!-- Populated as prompts 08–10 land. Each entry: name, purpose, location, producer prompt. -->

*(none yet — populated starting prompt 08)*

## Commands authored so far

<!-- Populated as prompt 11 lands. Each entry: command, purpose, file, producer prompt. -->

*(none yet — populated in prompt 11)*

## Agents authored so far

<!-- Populated as prompts 05–07 land. -->

*(none yet — populated starting prompt 05)*

## Hooks configured so far

<!-- Populated as prompt 12 lands. -->

*(none yet — populated in prompt 12)*

## Common failure modes and fixes

- **`ModuleNotFoundError: No module named 'pulsecraft'`** → you're running system `pytest` instead of venv. Use `.venv/bin/pytest`.
- **`No module named pip` in venv** → venv was created by `uv`, which omits pip. Use `uv pip install <pkg>` instead of `pip install`.
- **`pytest-asyncio` DeprecationWarning about event loop policy** → harmless, from pytest-asyncio internals. Ignore.
- **Python 3.14 compatibility issue with an LLM SDK** → pin to 3.13 via `uv venv --python 3.13` and rebuild venv. Flag to user before doing this.
- **Schema/Pydantic drift** (enum parity test fails) → one side was edited without the other. Align both and verify round-trip tests pass.

## What Claude Code should NOT do in this repo

- **Do not push to remote** unless the user explicitly asks.
- **Do not create branches.** Work on the current branch.
- **Do not invent design decisions.** If a prompt is ambiguous, ask the user.
- **Do not add `metadata: {}` escape-hatch fields to schemas.** If a shape is unknown, use a named sub-object with a TODO.
- **Do not add realistic-sounding fake names** (e.g., "John Smith"). Use explicit placeholders (`<head-alpha>`).
- **Do not commit real enterprise data of any kind** — names, system IDs, product names, internal URLs.
- **Do not skip verification steps** even when everything seems fine. The verification steps catch the subtle bugs.
- **Do not batch work across multiple prompts in one commit.** One prompt = one feature commit.
- **Do not silently work around SDK installation failures.** If `claude-agent-sdk` won't install, stop and ask.

## When in doubt

1. Re-read this file.
2. Re-read `design/planning/01-decision-criteria.md` if the question is about agent behavior.
3. Re-read `design/adr/ADR-002-subagent-topology.md` if the question is about where work belongs (subagent vs. skill vs. code).
4. Ask the user. Never guess on architecture.

---

*Last updated: prompt 03.5 (session continuity setup).*
*Next prompt: 04 — orchestrator spec (will extend this file with an "Orchestrator" section).*
````

### Step 3 — Update `design/planning/00-planning-index.md`

The planning index currently reflects post-P2 state. Bring it to current state:

1. In the **"Current phase"** section at the top, change to:
   > **P3 — Agent prompt authoring (in progress).** Planning complete (P0-P2). Infrastructure phase (schemas P4, config P5 retroactively completed in prompt sequence 02-03). Session continuity established in prompt 03.5.

   > Actually — rethink this. The phase naming I used earlier (P0-P9) and the prompt numbering (00-14) are different things. Don't conflate them. The phase names refer to conceptual milestones from the original planning doc; the prompt numbers are the build sequence. In the current-phase section, use this instead:
   >
   > **Phase:** Active implementation — schemas and config complete, agent authoring next.
   >
   > **Last completed:** Prompt 03 (config + fixtures) + 03.5 (session continuity).

2. In the **Planning phases** table, mark P3 as in progress with a note that schemas (P4 per old numbering) and config (P5 per old numbering) are actually complete because the prompt sequence pulled them forward.

3. In the **Completed artifacts** table, add:
   - Entry for data contracts / schemas (prompt 02 output)
   - Entry for config + fixtures (prompt 03 output)
   - Entry for session continuity / CLAUDE.md (prompt 03.5 output)

4. In the **Prompt-driven build workflow** table, update statuses:
   - 00, 01, 02, 03 → ✅ Done
   - 03.5 → ✅ Done (add this row)
   - 04 → ⏳ Next

### Step 4 — Update root `README.md`

In the root `README.md`, find the "Status" indicator (or wherever the current phase is noted). Update to reflect:

- "Planning phase complete. Implementation in progress."
- "Latest: schemas + config + fixtures committed. Agent prompt authoring begins in prompt 04."
- Link to `CLAUDE.md` with a note: "For Claude Code sessions: read `CLAUDE.md` for standing instructions."

Don't rewrite the whole README. Just update the current-status section.

### Step 5 — Fix the `types-PyYAML` TODO from prompt 03

Open `pyproject.toml`. Under `[project.optional-dependencies].dev`, add `types-PyYAML` if it's not already listed. Keep the version constraint style consistent with the other dev deps (if they use `~=`, use `~=`; if they use `>=`, use `>=`).

After adding, run `uv pip install -e ".[dev]"` (or `.venv/bin/python -m pip install -e ".[dev]"` if uv isn't available) to make sure the declared dep matches installed state.

### Step 6 — Verify

Run in order using venv binaries:

1. `.venv/bin/ruff check .` — passes
2. `.venv/bin/ruff format --check .` — passes
3. `.venv/bin/mypy src/pulsecraft/` — passes (types-PyYAML now declared, should not surface new issues)
4. `.venv/bin/pytest tests/ -v` — 84 tests still pass (no new tests in this prompt, but nothing should regress)
5. Sanity-check that `CLAUDE.md` exists at repo root, is non-empty, and starts with `# CLAUDE.md — Standing Instructions`
6. Sanity-check that `design/planning/00-planning-index.md` references prompt 03.5

### Step 7 — Commit

Stage all four modified files (`CLAUDE.md` new, `design/planning/00-planning-index.md` modified, `README.md` modified, `pyproject.toml` modified) and commit with:

```
chore: establish session continuity (CLAUDE.md, planning index, types-PyYAML)

- Add CLAUDE.md at repo root as standing instructions for every Claude Code
  session. Includes: project context, current phase tracker, source-of-truth
  links, environment rules, conventions, post-commit update discipline,
  common failure modes, and what-not-to-do list.
- Update design/planning/00-planning-index.md to reflect prompts 00-03 and
  this prompt (03.5) as done. Prompt 04 flagged as next.
- Update root README status indicator and add pointer to CLAUDE.md.
- Declare types-PyYAML as a dev dependency in pyproject.toml (was installed
  but not declared; flagged as TODO in prompt 03 final report).

This prompt establishes the pattern: every future prompt updates CLAUDE.md
and the planning index as part of its commit, so the repo stays
self-documenting across Claude Code sessions.

Next: prompt 04 — orchestrator spec (extends CLAUDE.md with an
"Orchestrator" section; does not replace existing content).
```

Do not push to remote unless the user asks.

## Rules for this session

- **Do not modify any schema, config, or fixture file.** Those are fixed from prompts 02 and 03.
- **Do not author any new code** beyond the pyproject.toml line.
- **Preserve the CLAUDE.md template structure exactly.** Future prompts will look for specific sections (`## Current phase`, `## Skills authored so far`, etc.) and append to them. If sections are renamed or reordered, downstream prompts break.
- **If you notice any other gaps** (missing dep declaration, stale comment, drifting reference), add them to the final report as a suggestion rather than fixing ad-hoc.

## Final report

1. **Files created/modified** — full list with byte sizes or line counts.
2. **Verification results** — each step pass/fail.
3. **Any gaps noticed** but not fixed (for user review).
4. **Commit hash** — `git log -1 --format="%h %s"`.
5. **Next prompt** — "Ready for prompt 04: orchestrator spec (will extend CLAUDE.md)."

---

## [Post-commit] Save this prompt file to the repo

After the main commit lands, ask the user: **"Save this prompt file (prompt 03.5) to `prompts/03.5-session-continuity.md` as a commit archive? (yes/no)"**

If yes:
- The user will provide the prompt file content or path.
- Write it verbatim to `prompts/03.5-session-continuity.md`.
- Commit with: `chore(prompts): archive prompt 03.5 (session continuity) in repo`.

If no: skip.
